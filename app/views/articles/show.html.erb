<section id="one">
	<div class="container">
		<header class="major">
			<h2><%= @article.title %></h2>
			<p></p>
		</header>
		
		<p><%= @article.text %></p>
	</div>
</section>

<section id="two">
	<div class="container">
	    <h3>Comments</h3>
	    <div class="comments">
            <%= render @article.comments %>
        </div>
        
        <hr/>
        
	    <h3>Add a comment:</h3>
        <%= render 'comments/form' %>
	</div>
</section>
