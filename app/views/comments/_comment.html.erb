<article>
    <div class="image"><%= image_tag "user.png" %></div>
    <div class="inner">
	    <h4><%= comment.commenter %></h4>
	    <p>
	        <%= comment.body %><br/>
	        <%= link_to 'Delete', [comment.article, comment],
                       method: :delete,
                       data: { confirm: 'Are you sure?' } %>
        </p>
    </div>
</article>
